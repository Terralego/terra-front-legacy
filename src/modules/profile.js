const initialState = {
  email: '',
  paddword: '',
  properties: {
    firstname: '',
    lastname: '',
  },
};

/**
 * REDUCER
 * --------------------------------------------------------- *
 */
const profile = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default profile;


/**
 * ACTIONS
 * --------------------------------------------------------- *
 */
